const assert=require('node:assert/strict'),fs=require('node:fs');const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));const releases=JSON.parse(fs.readFileSync('public/releases.json','utf8'));const notes=fs.readFileSync('RELEASE_NOTES.md','utf8');assert.equal(pkg.version,'0.49.0');assert.deepEqual(releases.versions.flatMap(x=>x.builds).filter(x=>x.current).map(x=>x.version),['0.49.0']);assert.match(notes,/410 × 700/);assert.match(notes,/30.Sekunden/);assert.match(fs.readFileSync('electron/main.ts','utf8'),/resolveSplashWindowBounds/);console.log('Version 0.49 startup screen checks passed.');
