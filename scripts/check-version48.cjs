const assert=require('node:assert/strict'),fs=require('node:fs');const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));const releases=JSON.parse(fs.readFileSync('public/releases.json','utf8'));const notes=fs.readFileSync('RELEASE_NOTES.md','utf8');assert.ok(Number(pkg.version.split('.')[1])>=48);assert.ok(releases.versions.flatMap(x=>x.builds).some(x=>x.version==='0.48.0'));for(const phrase of ['Desktop-Größe','Medienbibliothek','KI-Motive','Bibeltext anzeigen','MAIN-Schnellanzeige'])assert.match(notes,new RegExp(phrase));console.log('Version 0.48 release checks passed.');
