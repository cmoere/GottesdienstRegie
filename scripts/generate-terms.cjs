const fs=require('node:fs'),path=require('node:path'),ts=require('typescript'),Module=require('node:module');const file=path.resolve('src/termsContent.ts'),mod=new Module(file,module);mod._compile(ts.transpileModule(fs.readFileSync(file,'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText,file);const t=mod.exports,escape=s=>s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));fs.writeFileSync('build/terms.txt',t.plainTerms());fs.mkdirSync('public/terms',{recursive:true});const sections=t.termsSections.map((s,i)=>`<section id="section-${i+1}"><h2>${escape(s.title)}</h2>${s.paragraphs.map(p=>`<p>${escape(p)}</p>`).join('')}</section>`).join('');const nav=t.termsSections.map((s,i)=>`<a href="#section-${i+1}">${escape(s.title)}</a>`).join('');fs.writeFileSync('public/terms/index.html',`<!doctype html><html lang="de"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Nutzungsbedingungen – GottesdienstRegie</title><link rel="stylesheet" href="terms.css"></head><body><header><strong>GottesdienstRegie</strong><h1>Nutzungsbedingungen</h1><p>Version ${escape(t.TERMS_VERSION)} · Gültig ab ${escape(t.TERMS_EFFECTIVE_DATE)}</p></header><div class="layout"><nav aria-label="Inhalt">${nav}</nav><main>${sections}</main></div></body></html>`);console.log(`Generated terms ${t.TERMS_VERSION}`);
