import{useMemo,useState}from'react';import{languageByCode,searchLanguages}from'./languageCatalog';import{downloadTranslationPack,isTranslationPackInstalled}from'./translationPackManager';
export function LanguagePicker({value,onChange,source='en',disabled=false}:{value:string;onChange:(code:string)=>void;source?:string;disabled?:boolean}){const[open,setOpen]=useState(false),[query,setQuery]=useState(''),[progress,setProgress]=useState<Record<string,number>>({}),languages=useMemo(()=>searchLanguages(query),[query]),selected=languageByCode(value);return <div className="language-picker"><button type="button" disabled={disabled} onClick={()=>setOpen(v=>!v)}><span className="language-code">{selected.code.toUpperCase()}</span> {selected.name} <span className="material-symbols-outlined" aria-hidden="true">arrow_drop_down</span></button>{open&&<div className="language-picker-popover" role="dialog" aria-label="Übersetzungssprache"><input autoFocus placeholder="Sprache suchen …" value={query} onChange={e=>setQuery(e.target.value)}/><div>{languages.map(language=>{const ready=isTranslationPackInstalled(source,language.code),busy=progress[language.code];return <button type="button" key={language.code} onClick={async()=>{if(!ready){setProgress(p=>({...p,[language.code]:1}));await downloadTranslationPack(source,language.code,percent=>setProgress(p=>({...p,[language.code]:percent})));setProgress(p=>({...p,[language.code]:0}))}onChange(language.code);setOpen(false)}}><span className="language-code">{language.code.toUpperCase()}</span><span><b>{language.name}</b><small>{language.nativeName}</small></span><span>{busy?`${busy}%`:ready?<span className="material-symbols-outlined" aria-label="Bereit">check_circle</span>:<span className="material-symbols-outlined" aria-label="Herunterladen">cloud_download</span>}</span></button>})}</div></div>}</div>}
