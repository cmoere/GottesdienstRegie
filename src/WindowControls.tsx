import {useEffect,useState} from 'react';
export function WindowControls(){const [full,setFull]=useState(false);useEffect(()=>{const api=(window.desktop as any)?.operator;void api?.fullscreen?.().then(setFull);return api?.onFullscreen?.(setFull)},[]);return full?<div className="fullscreen-controls"><button aria-label="Minimieren" onClick={()=>(window.desktop as any)?.operator.control('minimize')}>—</button><button aria-label="Vollbild verlassen" onClick={()=>(window.desktop as any)?.operator.control('restore')}>❐</button><button aria-label="Programm schließen" onClick={()=>(window.desktop as any)?.operator.control('close')}>×</button></div>:null}
