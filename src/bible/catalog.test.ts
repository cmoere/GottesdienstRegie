import {describe,expect,it} from 'vitest';import {BIBLE_CATALOG,visibleBibleEntries} from './catalog';describe('Bible catalog',()=>{it('models 26 German and four English translations',()=>{expect(BIBLE_CATALOG.filter(x=>x.language==='de')).toHaveLength(26);expect(BIBLE_CATALOG.filter(x=>x.language==='en').map(x=>x.shortName)).toEqual(['KJV','ASV','WEB','BSB'])});it('hides licensed packages and defaults to Luther',()=>{const visible=visibleBibleEntries();expect(visible[0].id).toBe('luther1912');expect(visible.every(x=>x.availability!=='licensed')).toBe(true)});});
